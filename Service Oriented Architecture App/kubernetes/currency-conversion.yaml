apiVersion: apps/v1
kind: Deployment
metadata:
  name: currency-conversion-deployment
  labels:
    app: currency-conversion
spec:
  replicas: 1
  selector:
    matchLabels:
      app: currency-conversion
  template:
    metadata:
      labels:
        app: currency-conversion
    spec:
      initContainers:
        - name: wait-for-eureka
          image: busybox
          command: ['sh', '-c', 'until nc -z naming-server-service 8761; do echo waiting for eureka...; sleep 5; done']
      containers:
      - name: currency-conversion
        image: exchange-app/currency-conversion
        ports:
        - containerPort: 8100
        envFrom:
        - configMapRef:
            name: eureka-cm
---
apiVersion: v1
kind: Service
metadata:
  name: currency-conversion-service
spec:
  selector:
    app: currency-conversion
  ports:
  - protocol: TCP
    port: 8100
    targetPort: 8100
